"use client";
import {useEffect,useState} from 'react';
import type dossier from './dossier.json';
export default function ResearchPage(){const [data,setData]=useState<typeof dossier|null>(null);useEffect(()=>{import('./dossier.json').then(d=>setData(d.default))},[]);return <section id="research" className="section editorial-page"><span className="eyebrow">RESEARCH PROGRAMMES</span><h2>From the instrument<br/>to public health.</h2><p className="editorial-intro">Engineering, microbiology and policy questions behind AMReye.AI. Each topic separates the proposal, its evidence and the work still required.</p><div className="research-grid">{data?.topics.filter(t=>t.id.startsWith('research-')||t.id==='topic-60').map(t=><a key={t.id} href={`/?topic=${t.id}#apex`}><small>{t.category} · {t.status}</small><h3>{t.title}</h3><p>{t.text[0]}</p><span>Read topic and sources ↗</span></a>)}</div><aside className="offline-card"><h3>Keep the library available offline.</h3><p>The saved reading edition contains all 100 topics and the source register. Open it once while connected. This is a reference reader; laboratory control and cloud synchronisation remain proposed capabilities.</p><a className="solid-link" href="/offline.html">Open offline reading edition ↗</a></aside></section>}
